% Footnote formatting improvements (hyperref-compatible)
\usepackage{soul}

% Configure footnotes to wrap properly and stay within margins
\interfootnotelinepenalty=10000

% Footnote spacing (without footmisc which breaks hyperref footnote links)
\setlength{\footnotesep}{0.5em}

% Force line breaks in long URLs inside footnotes
\usepackage{url}
\def\UrlBreaks{\do\/\do-\do.\do=\do_\do?\do&\do;\do,\do!\do:\do+}
\urlstyle{same}
